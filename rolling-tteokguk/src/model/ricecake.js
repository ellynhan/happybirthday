export default class Ricecake {
    constructor(id, name, message,left, top) {
      this.id = id;
      this.name = name;
      this.message = message;
      this.left = left;
      this.top = top;
    }
  }